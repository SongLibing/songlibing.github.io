---
---
{%- capture srcbase %}src="{{ site.url }}/{% endcapture -%}
{%- capture hrefbase %}href="{{ site.url }}/{% endcapture -%}
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="zh-CN">
  <generator uri="https://jekyllrb.com/">Jekyll</generator>
  <title type="html">{{ site.title | xml_escape }} (中文)</title>
  <subtitle>{{ site.social.name | xml_escape }} 的中文文章。</subtitle>
  <link href="{{ site.url }}/feed-zh.xml" rel="self" type="application/atom+xml"/>
  <link href="{{ site.url }}/" rel="alternate" type="text/html"/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ site.url }}/feed-zh.xml</id>
  <author>
    <name>{{ site.social.name | xml_escape }}</name>
  </author>
  {%- assign zh_posts = site.posts | where_exp: "post", "post.lang != 'en'" -%}
  {%- for post in zh_posts limit: 30 %}
  <entry>
    <title type="html">{{ post.title | xml_escape }}</title>
    <link href="{{ site.url }}{{ post.url }}" rel="alternate" type="text/html" title="{{ post.title | xml_escape }}"/>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.last_modified_at | default: post.date | date_to_xmlschema }}</updated>
    <id>{{ site.url }}{{ post.url }}</id>
    <author>
      <name>{{ site.social.name | xml_escape }}</name>
    </author>
    {%- for tag in post.tags %}
    <category term="{{ tag | xml_escape }}"/>
    {%- endfor %}
    <summary type="html">{{ post.content | strip_html | truncatewords: 60 | xml_escape }}</summary>
    <content type="html" xml:base="{{ site.url }}{{ post.url }}">{{ post.content | replace: 'src="/', srcbase | replace: 'href="/', hrefbase | xml_escape }}</content>
  </entry>
  {%- endfor %}
</feed>
